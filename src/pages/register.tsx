import { Search } from "components/data/Search";
import { Layout } from "components/Layout";

export default function Register() {
  return (
    <Layout>
      <Search name="Registrar imóvel" />
    </Layout>
  );
}
